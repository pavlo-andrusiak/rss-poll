{
  "name": "rss-poll",
  "version": "1.0.0",
  "description": "Simple Node.js app that polls an RSS feed at a configurable interval.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "type": "module",
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2",
    "xml2js": "^0.6.2"
  }
}
